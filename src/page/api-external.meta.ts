export const generatePostUrl = (postId: number) =>
  `https://jsonplaceholder.typicode.com/posts/${postId}`;

export const generateUserUrl = (userId: number) =>
  `https://jsonplaceholder.typicode.com/users/${userId}`;
